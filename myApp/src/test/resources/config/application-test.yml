spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    hikari:
      maximum-pool-size: 2
    password: ''
    url: jdbc:tc:postgresql:18.2:///bank?TC_TMPFS=/testtmpfs:rw
    username: bank
  liquibase:
    contexts: test
  main:
    banner-mode: 'off'
logging:
  config: classpath:logback.xml
server:
  port: 0
